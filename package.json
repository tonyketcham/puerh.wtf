{
  "name": "puerh.wtf-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter app dev",
    "build": "pnpm --filter app build && pnpm --filter log build",
    "test": "pnpm --filter app test"
  },
  "pnpm": {
    "peerDependencyRules": {
      "allowedVersions": {
        "graphql": "^16.0.1"
      }
    }
  },
  "dependencies": {
    "@tailwindcss/line-clamp": "^0.4.4"
  }
}
